Atividade2 BD2 Caua Mariz, Gustavo Santos
2DS 27/06/2025

create database atividade2;
use atividade2;

create table album (
codigo_album tinyint auto_increment primary key,
nome varchar(150) not null

);

create table cromo(
codigo_cromo tinyint primary key,
numero int not null,
descricao varchar(150),
codigo_album tinyint not null,
foreign key (codigo_album)references album(codigo_album)
);

create table fabricacao(
numero_fabricacao smallint auto_increment primary key,
quantidade int not null,
data_fabricacao date not null,
codigo_cromo tinyint not null,
 foreign key (codigo_cromo)references cromo(codigo_cromo)
);


create table produto(
id_produto int auto_increment primary key,
nome_produto varchar(100) not null
);


create table nfiscal(
id_nota int auto_increment primary key,
valor smallint not null,
data_venda date not null,
id_produto int not null,
foreign key(id_produto) references produto(id_produto)
);


create table cliente(
id_cliente int auto_increment primary key,
nome_cli varchar(150) not null,
id_nota int,
 foreign key (id_nota)references nfiscal(id_nota)
);


create table fabricacao(
id int auto_increment primary key,
descricao varchar(150) not null
);


create table produto(
id int auto_increment primary key,
descricao varchar(150) not null,
ativo boolean,
qtd int not null,
id int not null,
 foreign key (id)references fabricacao(id)
);


create table pedido(
id int auto_increment primary key,
quantidade int not null,
data_pedido date not null,
id int not null,
foreign key (id)references produto(id)
);


create table fornecedor(
id int auto_increment primary key,
nome varchar(150)
);


create table cliente(
cpf bigint auto_increment primary key,
endereco varchar(150) not null,
nome varchar(150) not null
);


create table pedido(
numero int auto_increment primary key,
status_pedido boolean not null,
endereco_entrega varchar(150) not null,
data_pedido date not null,
cpf bigint not null,
 foreign key (cpf)references cliente(cpf)
);


create table item_pedido(
qtde varchar(150) not null,
numero int not null,
 foreign key (numero)references pedido(numero)
);


create table produto(
codigo tinyint auto_increment primary key,
preco int not null,
nome varchar(150) not null,
qtde varchar(150) not null,
 foreign key (qtde)references item_pedido(qtde)
);



create table categoria(
codigo_cat int primary key auto_increment,
nome varchar(150)
);


e)

create table carro(
codCar int primary key auto_increment,
placa varchar(10) not null,
cor varchar(150),
ano varchar(150) ,
diaria decimal(10,2),
modelo varchar(150) not null
);

create table cliente(
codCli int primary key auto_increment,
nome varchar(150),
cnh varchar(150) not null,
endereco varchar(150) not null,
telefone varchar(50)
);

create table esportivo(
codCar int primary key,
velocidade int,
foreign key(codCar) references Carro(codCar)
);

create table sedan(
codCar int primary key,
passageiros int,
foreign key(codCar) references Carro(codCar)
);

create table aluga(
codAlu madiumint primary key auto_increment,
dataInicial date not null,
datafinal date not null,
codCar int,
cpf bigint,
foreign key(codCar) references Carro(codCar),
foreign key(cpf) references Cliente(cpf)
);